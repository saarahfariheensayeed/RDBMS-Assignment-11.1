7)
SELECT MAX(LENGTH),CATEGORY FROM FILM_LIST;
	MAX(LENGTH)	CATEGORY
	185		Documentary
8)
SELECT FILM.TITLE,COUNT(DISTINCT ACTOR_ID) FROM FILM
JOIN FILM_ACTOR USING
(FILM_ID)
GROUP BY FILM_ID
LIMIT 10;
	TITLE			COUNT(DISTINCT ACTOR_ID)
	ACADEMY DINOSAUR		10
	ACE GOLDFINGER			4
	ADAPTATION HOLES		5
	AFFAIR PREJUDICE		5
	AFRICAN EGG			5
	AGENT TRUMAN			7
	AIRPLANE SIERRA			5
	AIRPORT POLLOCK		4
	ALABAMA DEVIL			9
	ALADDIN CALENDAR		8